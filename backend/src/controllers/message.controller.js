export const getUsersForSideBar = async (req, res) => {
    try {
        // Your own id for later not put in at the chat side bar
        const loggedInUserId = req.user._id;
    } catch (error) {
        
    }
}